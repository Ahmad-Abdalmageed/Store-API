{
  "license": "ISC",
  "scripts": {
    "client:install": "cd client && yarn install",
    "client:start": "cd client && yarn start",
    "client:build": "cd client && yarn build",
    "client:deploy": "cd client && yarn deploy",
    "server:install": "cd server && yarn install",
    "server:build": "cd server && yarn run build",
    "server:watch": "cd server && yarn run watch",
    "server:test": "cd server && yarn run test",
    "server:deploy": "cd server && yarn run deploy",
    "server:migrate-up": "cd server && yarn run up",
    "server:migrate-down": "cd server && yarn run down"
  }
}
